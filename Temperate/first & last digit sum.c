//4. Write C program to find sum of first and last digit of a number

#include<stdio.h>

main()
{
	int number;
	printf("Enter your number : ");
	scanf("%d",&number);
	
	int fd,ld;  
	for(number ; number>0 ; count++)
	{
		
		number = number/10;
	}
	
	printf("The sum of first and last digit of a number is : %d",count);
}